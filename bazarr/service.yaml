apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: jmedia
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.221"
    metallb.io/address-pool: "internal-pool"
spec:
  type: LoadBalancer
  selector:
    app: bazarr
  ports:
    - name: http
      port: 80
      targetPort: 6767
