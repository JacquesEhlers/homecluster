apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  namespace: jmedia
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.220"
    metallb.io/address-pool: "internal-pool"
spec:
  type: LoadBalancer
  selector:
    app: flaresolverr
  ports:
    - name: http
      port: 80
      targetPort: 8191
