apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: jmedia
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.219"
    metallb.io/address-pool: "internal-pool"
spec:
  type: LoadBalancer
  selector:
    app: prowlarr
  ports:
    - name: http
      port: 80
      targetPort: 9696
