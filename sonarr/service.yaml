apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: jmedia
  annotations:
    metallb.io/loadBalancerIPs: "192.168.2.217"
    metallb.io/address-pool: "internal-pool"
spec:
  type: LoadBalancer
  selector:
    app: sonarr
  ports:
    - name: http
      port: 80
      targetPort: 8989
